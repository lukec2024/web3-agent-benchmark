{
  "id": "nft_transfer",
  "version": "2.0.0",
  "category": "basic_transactions",
  "subcategory": "spl_token_ops",
  "difficulty": "medium",
  "title": "NFT Transfer",
  
  "natural_language_templates": [
    "Transfer NFT {nft_address} to address {to_address}",
    "Send my NFT {nft_address} to {to_address}",
    "Transfer NFT {nft_address} to {to_address}",
    "I want to transfer NFT (token mint: {nft_address}) to {to_address}"
  ],
  
  "description": [
    "Transfer an NFT token to another address using the transferFrom function.",
    "",
    "Technical context:",
    "- Function signature: transferFrom(address from, address to, uint256 tokenId)",
    "- The 'from' address must be the current owner of the NFT",
    "- The caller must be the owner or approved operator",
    "- Recommended gas limit: 100000",
    "",
    "Key considerations:",
    "- Use contract.interface.encodeFunctionData('transferFrom', [from, to, tokenId])",
    "- Set transaction 'to' to the NFT contract address",
    "- Set transaction 'value' to 0 (not sending BNB)",
    "- The 'from' parameter should be the agent's address (current owner)",
    "- After transfer, ownerOf(tokenId) should return the recipient address",
    "- The agent's NFT balance decreases by 1, recipient's increases by 1"
  ],
  
  "blockchain": {
    "chain": "Solana",
    "network": "mainnet"
  },
  
  "parameters": {
    "nft_address": {
      "type": "address",
      "role": "contract",
      "description": "NFT contract address",
      "generation": {
        "method": "from_list",
        "addresses": [
        ]
      }
    },
    "to_address": {
      "type": "address",
      "role": "recipient",
      "description": "Recipient address",
      "generation": {
        "method": "random"
      }
    }
  },
  
  "validation": {
    "pre_conditions": [
      {
        "type": "nft_ownership",
        "description": "Agent must own the NFT token",
        "nft_contract": "{nft_address}",
        "token_id": "{token_id}"
      }
    ],
    
    "post_conditions": [
      {
        "type": "transaction_success",
        "weight": 30,
        "description": "Transaction executed successfully"
      },
      {
        "type": "balance_change",
        "weight": 35,
        "description": "Token balance changed correctly",
        "expected": "1",
        "mint": "{nft_address}",
        "tolerance": 0
      },
      {
        "type": "balance_change",
        "weight": 35,
        "description": "Token balance changed correctly",
        "expected": "1",
        "compute": 1,
        "owner": "{to_address}",
        "mint": "{nft_address}",
        "tolerance": 0
      }
    ]
  },
  
  "metadata": {
    "tags": ["nft", "transfer", "ownership", "intermediate"],
    "estimated_gas": 100000,
    "requires_contract": true,
    "requires_nft_ownership": true,
    "contract_requirements": {
      "contract_type": "NFT",
      "required_functions": ["transferFrom", "ownerOf", "balanceOf"],
      "note": "Agent must own the NFT to transfer"
    }
  }
}

